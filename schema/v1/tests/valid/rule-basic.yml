apiVersion: ai-resource/v1
kind: Rule
metadata:
  id: no-secrets
  name: No Hardcoded Secrets
spec:
  enforcement: must
  scope:
    - files: ["**/*.py", "**/*.js"]
  body: "Never hardcode secrets in code"

apiVersion: ai-resource/v1
kind: Fragment
metadata:
  id: list-files
spec:
  inputs:
    files:
      type: array
      items:
        type: string
      required: true
    tasks:
      type: array
      items:
        type: object
        properties:
          name:
            type: string
          description:
            type: string
  body: "Process {{#files}}{{.}}{{/files}}"
